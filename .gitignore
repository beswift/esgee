# build output
bin/
obj/
publish/
Releases/

# local promote/probe scratch — real-settings.json inside carries the live
# PeerToken, so this directory must never be committable
build/

# user/IDE state
*.user
*.suo
.vs/
.idea/

# local agent state
.claude/

# macOS app — the project is generated from mac/project.yml (xcodegen)
mac/*.xcodeproj
mac/DerivedData/
mac/.build/
